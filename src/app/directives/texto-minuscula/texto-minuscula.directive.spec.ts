import { TextoMinusculaDirective } from './texto-minuscula.directive';

describe('TextoMinusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new TextoMinusculaDirective();
    expect(directive).toBeTruthy();
  });
});
